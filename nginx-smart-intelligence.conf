# Configuración Nginx para EC2
# Copiar a: /etc/nginx/conf.d/smart-intelligence.conf
# Reemplazar TU_DOMINIO con tu dominio real

upstream smart_intelligence {
    server localhost:8001;
}

server {
    listen 80;
    server_name TU_DOMINIO.com;

    client_max_body_size 100M;

    location / {
        proxy_pass http://smart_intelligence;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts para cargas grandes
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # WebSocket support
    location /ws {
        proxy_pass http://smart_intelligence;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}

